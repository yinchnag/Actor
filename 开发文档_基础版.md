# Actor1 开发文档（基础版）

## 1. 文档目标
本文件用于整理当前 Actor 框架的核心开发要求，作为后续实现与验收的统一依据。
本版先不考虑网络与存档能力，只覆盖可离线运行的核心并发调用与模块机制。

## 2. 当前范围

### 2.1 纳入本阶段
- 模块抽象与模块管理
- 模块函数反射调用
- 协程对象与任务投递
- 同协程与跨协程调用策略
- 基于协议号的模块函数分发逻辑（调用链保留，不接入网络层）

### 2.2 暂缓（后续阶段）
- 网络协议接入
- 发包能力
- 存档与载档的落地实现

## 3. 核心对象关系

### 3.1 角色
- IModule：单个业务模块接口
- ModObj[T]：IModule 的通用基类实现（组合继承使用）
- ITask：协程间调用任务抽象
- ChanTask：ITask 的任务载体实现
- IGoroutine：协程对象抽象
- IModLoader：Actor 统一模块加载与调用入口，且每个实例对应一个协程
- IUser：用户抽象（本阶段仅保留接口形态）
- IProtocol：协议抽象（本阶段仅保留消息分发形态）

### 3.2 关系约束
- 每个 Actor 对象需要实现 IModLoader。
- 每个 IModLoader 实例本质上是一个协程实例。
- 协程之间通过 chan 传递 ITask。
- 用户对象可被视为 IModLoader、IGoroutine、IUser 的组合能力承载体。

## 4. 接口整理与实现要点

## 4.1 IModule 与 ModObj[T]

IModule 的职责：
- 生命周期：Init、Update
- 通用调用：Invoke
- 协议映射：GetMetaHandler
- 脏标记：IsDirty、SetDirty
- Save、Load 接口保留，但本阶段不实现持久化细节

ModObj[T] 的职责：
- 通过反射收集宿主模块公共函数，建立 invokers
- 维护协议号到函数名映射 metaMsgHandler
- 维护宿主实例引用 heir，作为反射与调用主体
- 提供通用调用与协议分发支持

关键约束：
- 业务模块采用组合继承：在结构体中嵌入 ModObj[具体类型]。
- 业务模块使用前必须先调用 Init，避免 heir 未初始化导致空引用。
- 协议响应函数识别规则：单参数、无返回值，且参数类型在协议注册表可识别。

建议最小实现：
- Init：初始化 invokers 和 metaMsgHandler，然后执行 setInvokerAll
- Invoke：按函数名查找并反射调用，返回结果与错误
- Update：本阶段仅保留帧入口，不触发 Save
- Save、Load：保留空实现或显式返回“未启用”状态（根据实际编码风格选择）

## 4.2 ITask 与 ChanTask

ITask 职责：
- 描述一次跨协程或同协程调用请求
- 记录调用来源、目标、模块名、函数名、参数、结果、状态

ChanTask 建议状态流转：
- 0：可回收/已释放
- 1：执行中
- 2：执行完成

最小行为：
- Getter 系列函数完整实现
- Await：等待任务结束
- WithTimeout：设置超时等待
- cancel：中断等待阻塞

本阶段约束：
- 只保证任务编排和等待机制可用
- 不引入网络上下文

## 4.3 IGoroutine

IGoroutine 职责：
- 维护协程身份与关闭状态
- 暴露任务输入通道
- 提供 RunUpdateLoop 作为主循环

RunUpdateLoop 最小行为：
- 使用 select 处理两类事件：
  - 定时帧更新（例如每秒触发一次）
  - 外部任务通道输入的 ITask
- 收到 ITask 后执行模块调用并回填结果

注意事项：
- SetGoroutineID 可在初始化时执行一次，避免高频调用 runtime.Stack
- 主循环要确保可控退出，避免 WaitGroup 泄漏

## 4.4 IModLoader（核心）

IModLoader = IGoroutine + 模块管理与路由能力。

职责拆分：
- Init：初始化模块容器与协程状态
- AddModule：注册模块
- GetModule：按名称获取模块
- ModInvoke：统一调用入口（同协程直调，跨协程投递任务）
- OnMessageHandler：根据协议号找到模块函数，并通过 ModInvoke 执行

关键策略：
- 同协程调用：直接 Invoke
- 跨协程调用：封装 ChanTask 并投递到目标协程通道
- 存在返回值时：调用方阻塞等待任务完成

## 5. 关键流程（无网络版）

## 5.1 模块初始化流程
1. 创建 IModLoader 实例并 Init。
2. 创建业务模块实例（组合 ModObj[T]）。
3. 调用模块 Init，完成反射元数据装载。
4. AddModule 注册到 Loader。
5. 启动 RunUpdateLoop。

## 5.2 函数调用流程
1. 调用方发起 ModInvoke(模块名, 方法名, 参数)。
2. Loader 判断是否跨协程。
3. 若不跨协程：直接调用目标模块 Invoke。
4. 若跨协程：封装 ChanTask 投递到目标协程。
5. 若调用需要返回值：调用方 Await 等待完成并读取结果。

## 5.3 协议分发流程（离线模拟）
1. 输入一个 IProtocol（可由测试桩构造，不走网络）。
2. OnMessageHandler 遍历模块协议映射。
3. 命中协议号后，通过 ModInvoke 调用目标函数。
4. 调用路径与跨协程规则保持一致。

## 6. 建议目录（示例）
- core/module：IModule、ModObj、反射工具
- core/task：ITask、ChanTask
- core/goroutine：IGoroutine 及运行循环
- core/loader：IModLoader 及实现
- core/protocol：IProtocol、协议注册表与测试桩
- examples：最小可运行示例

## 7. 本阶段验收标准
- 可以注册多个模块并按名称调用。
- 可以识别同协程与跨协程调用路径。
- 跨协程调用可正确回填结果并解除等待。
- OnMessageHandler 可根据协议号路由到模块函数（可使用离线协议对象）。
- 在不接入网络与存档的前提下，完整跑通主循环与调用链。

## 8. 后续扩展位
- 存档接入：启用 Save、Load 与脏标记落地。
- 网络接入：IUser.SendMessage 与真实协议收发。
- 任务治理：超时、取消、重试、监控指标。
- 性能治理：反射缓存、任务池化、协程负载观测。
