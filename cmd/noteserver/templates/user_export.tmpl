{{- /*
每用户 actor 的门面模板。

【什么时候用这份】
模块挂在**每用户一个**的 actor 上时用它——也就是所有 **_mod.go，本项目里是
NoteMod。这条没有例外：每用户 actor 的取用/归还是硬性的，见下。
挂在服务器上的 **_mgr.go 不要用这份，看 shard_export.tmpl 的说明。

【怎么调用】
不要手敲命令。对应关系写在模块文件的 //go:generate 那一行里，那也是
gercmd verify 读的地方——两边一旦分岔，校验就失去意义。所以统一：

    go generate ./src/mods/...

模块文件里那行长这样。注意它**必须是一行**——go:generate 不认 \ 折行，
折了后半截会被当成普通注释、参数悄悄丢掉。路径相对仓库根，因为 -C 先切了过去：

    //go:generate go -C ../../../../.. run ./cmd/gercmd gen -force -tmpl cmd/noteserver/templates/user_export.tmpl -recv Hub cmd/noteserver/src/mods/note/note_mod.go cmd/noteserver/src/service

【与 shard_export.tmpl 的区别】
只有取 loader 那一步，但那一步是这份模板存在的全部理由：每用户 actor 是
**按需创建、空闲回收**的，取用必须配对 AcquireUser / ReleaseUser——inFlight
计数就是靠这一对守住"正在服务的 actor 不被回收"。漏一次 Release，那个 actor
的计数永远回不到 0，从此再也不会被回收，一个用户漏一条协程，而且不报错。

原先这一对写在每个 HTTP handler 里，靠人记得写 defer。收进生成的门面之后，
它不再是"要记得做的事"，而是"想漏也漏不掉"。

【签名】
模块方法本身不带 uid（一个 NoteMod 只服务一个账号，uid 构造时就定死了），
所以门面要额外接一个 uid 参数用来选 actor——签名里它排在 gid 之后。

【对模块方法的约定】
要么没有返回值（投递即忘），要么正好是 (T, error)。
违反会生成非法 Go 代码、gen 当场失败。
*/ -}}
// Code generated by gercmd from {{.Source}}; DO NOT EDIT.
// 改动请改模块方法上的 export: 标记后重新生成：go generate ./src/mods/

package {{.Package}}

{{if .Imports -}}
import (
{{range .Imports}}	"{{.}}"
{{end}})
{{end}}

{{- range .Funcs}}
{{- $fn := .}}
{{- if and .HasResults (ne .NumResults 2)}}
#此模板要求导出方法的返回值是 (T, error)，{{.ModType}}.{{.Method}} 有 {{.NumResults}} 个
{{- end}}

{{.DocText -}}
func (that *{{$.Recv}}) {{.Name}}(gid uint64, uid string{{if .SigParams}}, {{.SigParams}}{{end}}){{.ResultSig}} {
	loader := that.AcquireUser(uid)
	defer that.ReleaseUser(uid)

{{- if .HasResults}}
	out, err := loader.ModInvokeFrom(gid, {{.InvokeArgs}})
	return unwrap[{{(index .Results 0).Type}}](out, err, "{{.ModType}}.{{.Method}}")
{{- else}}
	// 无返回值 = 投递即忘。投递失败框架已经报给 DiscardedErrorHandler，
	// 这里再返回一次只会让调用方重复处理同一件事。
	_, _ = loader.ModInvokeFrom(gid, {{.InvokeArgs}})
{{- end}}
}
{{end}}
