{{- /*
每用户 actor 的门面模板。

【什么时候用这份】
模块挂在**每用户一个**的 actor 上时用它——也就是所有 **_mod.go。
挂在服务器上的 **_mgr.go 看 shard_export.tmpl 的说明。

【它按有无返回值分成两种调用语义，这个分流本身就是规则】

  有返回值 → **请求**，只能由 rut 层调。
      走 AcquireUser：actor 不在就建一个。rut 跑在 gin 的请求协程上，
      不会被任何模块调用，阻塞它只影响这一个 HTTP 请求。

  无返回值 → **通知**，模块之间只能用这一种。
      走 TryUser：用户不在线就直接丢弃，**不创建 actor**。
      用 AcquireUser 去推会给一个刚被回收的用户重新拉起一条协程，
      把"在线用户数"悄悄变成"登录过的用户数"；而推送的语义本来就是
      "他在就给他"——数据在存储里，等他下次登录会重新拉。

为什么模块之间不能带返回值：框架的事件循环是单消费者，一个模块方法只要在等
另一个 actor 的返回值，它自己的队列就停着不动。两个模块互相等就是死锁，
而框架的环检测只覆盖同步调用，写成同步之后能救你的只有 3 秒超时。
所以 mgr 与 mod 之间每一跳都是单向通知，谁也不等谁。

代价要认：**请求与结果不在同一次往返里**。领取邮件只能回一个"已受理"，
客户端下次拉取才看到结果。真实游戏有长连接，最后那步是服务端推送；
HTTP 下只能靠客户端再问一次。

【怎么调用】
不要手敲命令，写在模块文件的 //go:generate 里，那也是 gercmd verify 读的地方。
指令必须是一行——go:generate 不认 \ 折行，折了后半截会被当成普通注释、参数
悄悄丢掉且不报错。

    //go:generate go -C ../../../../.. run ./cmd/gercmd gen -force -tmpl cmd/noteserver/templates/user_export.tmpl -recv Hub cmd/noteserver/src/mods/note/note_mod.go cmd/noteserver/src/service

【签名】
模块方法本身不带 uid（一个 Mod 只服务一个账号，uid 构造时就定死了），
所以门面要额外接一个 uid 参数用来选 actor——签名里它排在 gid 之后。

【对有返回值的方法的约定】
正好是 (T, error)。违反会生成非法 Go 代码、gen 当场失败。
*/ -}}
// Code generated by gercmd from {{.Source}}; DO NOT EDIT.
// 改动请改模块方法上的 export: 标记后重新生成：go generate ./src/mods/

package {{.Package}}

{{if .Imports -}}
import (
{{range .Imports}}	"{{.}}"
{{end}})
{{end}}

{{- range .Funcs}}
{{- $fn := .}}
{{- if and .HasResults (ne .NumResults 2)}}
#此模板要求导出方法的返回值是 (T, error)，{{.ModType}}.{{.Method}} 有 {{.NumResults}} 个
{{- end}}

{{.DocText -}}
func (that *{{$.Recv}}) {{.Name}}(gid uint64, uid string{{if .SigParams}}, {{.SigParams}}{{end}}){{.ResultSig}} {
{{- if .HasResults}}
	// 有返回值 = 请求，来自 rut。actor 不在就建一个。
	loader := that.AcquireUser(uid)
	defer that.ReleaseUser(uid)

	out, err := loader.ModInvokeFrom(gid, {{.InvokeArgs}})
	return unwrap[{{(index .Results 0).Type}}](out, err, "{{.ModType}}.{{.Method}}")
{{- else}}
	// 无返回值 = 通知，来自别的模块。用户不在线就丢弃，**不创建 actor**。
	loader := that.TryUser(uid)
	if loader == nil {
		return
	}
	defer that.ReleaseUser(uid)

	// 投递失败框架已经报给 DiscardedErrorHandler，这里再返回一次只会让
	// 调用方重复处理同一件事。
	_, _ = loader.ModInvokeFrom(gid, {{.InvokeArgs}})
{{- end}}
}
{{end}}
